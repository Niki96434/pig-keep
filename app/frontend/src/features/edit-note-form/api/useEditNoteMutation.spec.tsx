describe('edit note', () => {
  it('should return success if title and content are empty')
  it('should return success if title and content are non-empty')
  it('should return error if title or content exceeded the limits')
  it('should return error if id is invalid')
})
